import { FC } from "react";
import InnerLayout from "@/components/layouts/inner-layout";

const ProfileScreen: FC = () => {
  return <InnerLayout withBack title="Настройки профиля" />;
};

export default ProfileScreen;
